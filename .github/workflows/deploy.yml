name: Deploy to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    name: Build
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup Node
        uses: actions/setup-node@v7
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Build Astro
        run: npm run build
        env:
          # Variables — wartości niechronione, widoczne w logach
          PUBLIC_CLOUDINARY_CLOUD_NAME:     ${{ vars.PUBLIC_CLOUDINARY_CLOUD_NAME }}
          PUBLIC_FORMSPARK_FORM_ID:         ${{ vars.PUBLIC_FORMSPARK_FORM_ID }}
          PUBLIC_FORMSPARK_EVENTS_FORM_ID:  ${{ vars.PUBLIC_FORMSPARK_EVENTS_FORM_ID }}
          PUBLIC_CLOUDINARY_API_KEY:        ${{ secrets.PUBLIC_CLOUDINARY_API_KEY }}
          PUBLIC_TURNSTILE_SITE_KEY:        ${{ vars.PUBLIC_TURNSTILE_SITE_KEY }}

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v5
        with:
          path: dist

  deploy:
    name: Deploy
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v5
